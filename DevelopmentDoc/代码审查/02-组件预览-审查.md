# 组件预览 (features/preview/) 代码审查报告

## 概述

本报告审查 LinkPro 项目中预览相关组件的实现，重点关注服务端/客户端代码边界、无限循环风险和组件拆分。

**审查日期**: 2026-01-29
**审查范围**: `src/components/features/preview/` 目录

---

## 目录

1. [layout-preview.tsx](#layout-previewtsx)
2. [aurora-preview-template.tsx](#aurora-preview-templatetsx)
3. [live-preview.tsx](#live-previewtsx)

---

## layout-preview.tsx

### 文件信息
- **路径**: `src/components/features/layout-preview/layout-preview.tsx`
- **行数**: 317 行
- **状态**: ⚠️ 需要改进

### 问题分析

#### 1. ✅ 无限循环已修复 (重要改进)

**修复位置**: 第 136 行

```typescript
}, [session?.user?.id]); // 移除 setModules 依赖，避免无限循环
```

**评价**: ✅ 正确修复
- 从依赖数组中移除了 `setModules`
- 添加了注释说明原因
- 这是之前导致应用卡死的关键问题

**历史问题** (已修复):
```typescript
// ❌ 之前的代码会导致无限循环
}, [session?.user?.id, setModules]); // setModules 引用不稳定

// ✅ 修复后的代码
}, [session?.user?.id]);
```

#### 2. ⚠️ 数据获取在客户端组件中重复

**问题** (第 64-136 行):
```typescript
useEffect(() => {
  let isMounted = true;
  let timeoutId: NodeJS.Timeout;

  async function loadData() {
    if (!session?.user?.id) {
      console.log("[LayoutPreview] No session, skipping load");
      setIsLoading(false);
      return;
    }

    // 添加超时保护
    timeoutId = setTimeout(() => {
      if (isMounted) {
        console.error("[LayoutPreview] Loading timeout!");
        setIsLoading(false);
      }
    }, 10000);

    try {
      console.log("[LayoutPreview] Starting data load...");
      setIsLoading(true);

      const [modulesResult, linksResult, profileResult] = await Promise.all([
        getModules(),
        getUserLinks(),
        getUserProfile(),
      ]);

      if (isMounted) {
        if (modulesResult.success) {
          // 不等待 setModules 完成，避免阻塞
          setModules(modulesResult.data).catch(err =>
            console.error("[LayoutPreview] setModules error:", err)
          );
        }
        // ...
      }
    } catch (error) {
      console.error("[LayoutPreview] Failed to load data:", error);
    } finally {
      clearTimeout(timeoutId);
      if (isMounted) {
        setIsLoading(false);
      }
    }
  }

  loadData();

  return () => {
    isMounted = false;
    if (timeoutId) clearTimeout(timeoutId);
  };
}, [session?.user?.id]); // 移除 setModules 依赖，避免无限循环
```

**问题分析**:

| 问题 | 影响 | 严重性 |
|------|------|--------|
| 数据在客户端获取 | 增加 TTI, 延迟内容展示 | 中 |
| 与其他组件重复获取相同数据 | 浪费网络请求, 性能损失 | 高 |
| setModules 不等待完成 | 可能导致状态不一致 | 中 |

**建议改进**:

```typescript
// ✅ 建议：在 Dashboard Layout (Server Component) 中获取数据
// src/app/(dashboard)/dashboard/layout.tsx

export default async function DashboardLayout({ children }: DashboardLayoutProps) {
  const session = await auth();
  if (!session?.user?.id) {
    redirect("/login");
  }

  // 并行获取所有初始数据
  const [modules, links, profile] = await Promise.all([
    getModules(),
    getUserLinks(),
    getUserProfile(),
  ]);

  return (
    <DashboardDataProvider
      initialModules={modules}
      initialLinks={links}
      initialProfile={profile}
    >
      <DashboardLayoutClient>{children}</DashboardLayoutClient>
    </DashboardDataProvider>
  );
}

// ✅ 然后 layout-preview.tsx 只需要从 context/store 读取
export function LayoutPreview({ /* props */ }: LayoutPreviewProps) {
  const { modules, links, profile } = useDashboardData();
  // 不再需要 useEffect 获取数据
}
```

#### 3. ⚠️ Store 使用不一致

**问题** (第 55-58 行):
```typescript
const { theme } = useEditorStore();
const { modules, mobileLayout } = useLayoutStore();
// 直接从 store 获取整个 state
const { setModules } = useLayoutStore();
```

**问题分析**:
- 第 58 行直接获取整个 state 而不是使用 selector hook
- 虽然在这种情况下不会导致问题（不在依赖数组中），但不是最佳实践

**建议**:
```typescript
// ✅ 使用专门的 selector hook
const setModules = useSetModules();
```

#### 4. ✅ 良好的错误处理和超时保护

```typescript
timeoutId = setTimeout(() => {
  if (isMounted) {
    console.error("[LayoutPreview] Loading timeout!");
    setIsLoading(false);
  }
}, 10000);
```

**评价**: ✅ 良好实践
- 防止无限 loading
- 正确的 cleanup

---

## aurora-preview-template.tsx

### 文件信息
- **路径**: `src/components/features/preview/aurora-preview-template.tsx`
- **行数**: 546 行 (最大文件)
- **状态**: ✅ 良好 (已重构)

### 评价

#### 1. ✅ 组件已正确拆分

```typescript
export function AuroraPreviewTemplate({ /* props */ }) {
  // 主组件逻辑
  return (
    <div>
      <AuroraBackground />
      <FlipButton onClick={handleFlip} />
      <FrontContent {...frontProps} />
      <BackContent {...backProps} />
    </div>
  );
}

// 提取的子组件
function AuroraBackground() { /* ... */ }
function FlipButton({ onClick, ariaLabel }) { /* ... */ }
function FrontContent({ /* props */ }) { /* ... */ }
function BackContent({ /* props */ }) { /* ... */ }
```

**评价**: ✅ 优秀的重构
- 提取了 4 个子组件
- 每个组件职责单一
- 遵循 Vercel `rendering-hoist-jsx` 最佳实践

#### 2. ✅ 使用 useCallback 稳定化函数

```typescript
// rerender-dependencies: 使用 useCallback 稳定化函数引用
const handleFlip = useCallback(() => {
  setIsFlipped((prev) => !prev);
}, []);

const handleKeyDown = useCallback((e: React.KeyboardEvent) => {
  if (e.key === 'Escape' && isFlipped) {
    setIsFlipped(false);
  }
}, [isFlipped]);
```

**评价**: ✅ 正确实现
- 避免不必要的子组件重渲染

#### 3. ✅ 动态导入减少 Bundle 大小

```typescript
// bundle-dynamic-imports: 动态导入图标减少 bundle 大小
const RotateCw = dynamic(() => import("lucide-react").then(mod => mod.RotateCw), {
  ssr: false,
  loading: () => <div className="w-5 h-5" />
});
```

**评价**: ✅ 符合最佳实践

#### 4. ✅ 静态 JS 常量提取

```typescript
// js-early-exit: 提取常量避免 Magic Numbers
const SAFE_AREA = {
  DYNAMIC_ISLAND_TOP: 12,
  DYNAMIC_ISLAND_HEIGHT: 37,
  // ...
} as const;
```

**评价**: ✅ 良好实践

---

## live-preview.tsx

### 文件信息
- **路径**: `src/components/features/preview/live-preview.tsx`
- **行数**: 251 行
- **状态**: 需要审查

### 问题分析

#### 1. ⚠️ 与 layout-preview.tsx 功能重叠

**发现**:
- `layout-preview.tsx` 和 `live-preview.tsx` 功能相似
- 都渲染模块预览
- 存在代码重复

**建议**: 合并或明确区分用途

---

## 总体建议

### 1. 数据获取架构重构

**当前流程** (有问题):
```
Dashboard 页面加载
    ↓
layout-preview.tsx 挂载
    ↓
useEffect 触发
    ↓
并行调用 getModules(), getUserLinks(), getUserProfile()
    ↓
setModules() 更新 store
```

**建议流程**:
```
Dashboard Layout (Server Component)
    ↓
并行获取所有初始数据
    ↓
传递给 Provider/Store
    ↓
layout-preview.tsx 直接使用数据
```

### 2. 移除客户端数据获取

**文件**: `src/components/features/preview/layout-preview.tsx`
**行数**: 64-136

```typescript
// ❌ 移除整个 useEffect
useEffect(() => {
  async function loadData() {
    // ... 数据获取逻辑
  }
  loadData();
}, [session?.user?.id]);

// ✅ 替换为从 context/store 读取
const { modules, links, profile, isLoading } = useDashboardData();
```

### 3. 创建统一的数据 Provider

```typescript
// src/contexts/dashboard-data-context.tsx
interface DashboardDataContextValue {
  modules: PageModule[];
  links: Link[];
  profile: UserProfile;
  isLoading: boolean;
  error: string | null;
  refresh: () => Promise<void>;
}

export const DashboardDataProvider = ({
  children,
  initialModules,
  initialLinks,
  initialProfile,
}: {
  children: React.ReactNode;
  initialModules?: ActionResult<PageModule[]>;
  initialLinks?: ActionResult<Link[]>;
  initialProfile?: ActionResult<UserProfile>;
}) => {
  const [state, setState] = useState<DashboardDataContextValue>({
    modules: initialModules?.data ?? [],
    links: initialLinks?.data ?? [],
    profile: initialProfile?.data ?? defaultProfile,
    isLoading: false,
    error: null,
  });

  // 提供刷新功能
  const refresh = async () => {
    // 重新获取数据
  };

  return (
    <DashboardDataContext.Provider value={{ ...state, refresh }}>
      {children}
    </DashboardDataContext.Provider>
  );
};
```

---

## 优先级修复列表

| 优先级 | 问题 | 文件 | 行数 |
|--------|------|------|------|
| 🔴 高 | 客户端重复获取数据 | `layout-preview.tsx` | 64-136 |
| 🔴 高 | 创建统一数据 Provider | 新文件 | - |
| 🟡 中 | Store 使用不一致 | `layout-preview.tsx` | 58 |
| 🟡 中 | 合并重复的预览组件 | `live-preview.tsx` | 全部 |

---

## 相关 Vercel React Best Practices 违规

1. **client-swr-dedup**: 缺少请求去重
2. **async-parallel**: 数据获取可以在服务端并行化
3. **server-cache-react**: 可以使用 React.cache() 去重

---

**审查人员**: Claude AI
**最后更新**: 2026-01-29
