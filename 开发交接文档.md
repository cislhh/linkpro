# LinkPro 开发交接文档

## 项目概述

LinkPro 是一个下一代个人品牌主页生成器，让开发者和创作者能够通过拖拽和配置，快速生成拥有高级动效和个性化设计的社交名片工具。区别于静态的 Linktree，LinkPro 强调视觉冲击力和个性化表达。

---

## 技术栈

| 类别 | 技术选型 |
|------|----------|
| 前端框架 | Next.js 16 (App Router + React Server Components) |
| 语言 | TypeScript |
| 样式方案 | Tailwind CSS + shadcn/ui |
| 状态管理 | Zustand |
| 数据库 | PostgreSQL + Prisma ORM |
| 认证 | NextAuth.js (Auth.js v5) |
| 数据验证 | Zod |
| 动画 | Framer Motion |
| 拖拽布局 | react-grid-layout + @dnd-kit |
| 测试 | Vitest + fast-check (属性测试) |

---

## 项目结构

```
src/
├── app/                          # Next.js App Router 路由
│   ├── (auth)/                   # 认证页面组
│   │   ├── login/page.tsx        # 登录页
│   │   └── register/page.tsx     # 注册页
│   ├── (dashboard)/              # Dashboard 页面组
│   │   └── dashboard/
│   │       ├── page.tsx          # Dashboard 主页
│   │       ├── layout.tsx        # Dashboard 布局
│   │       ├── settings/         # 账户设置
│   │       ├── themes/           # 主题选择
│   │       ├── preview/          # 预览页面
│   │       └── layout-editor/    # 布局编辑器
│   ├── u/[username]/             # 公开个人主页 (SSR)
│   ├── api/auth/                 # NextAuth API 路由
│   ├── page.tsx                  # 首页 Landing Page
│   ├── layout.tsx                # 根布局
│   └── proxy.ts                  # 路由保护 (替代 middleware)
├── components/
│   ├── ui/                       # shadcn/ui 基础组件
│   ├── features/                 # 业务功能组件
│   │   ├── link-editor/          # 链接编辑器
│   │   ├── theme-selector/       # 主题选择器
│   │   ├── preview/              # 预览组件
│   │   ├── publish/              # 发布功能
│   │   ├── modules/              # 页面模块组件
│   │   ├── layout-editor/        # 布局编辑器组件
│   │   └── dashboard/            # Dashboard 组件
│   ├── themes/                   # 主题组件 (Aurora/Cyber/Glass)
│   └── providers/                # Context Providers
├── lib/                          # 工具库
│   ├── db.ts                     # Prisma 客户端单例
│   ├── auth.ts                   # NextAuth 配置
│   ├── validations.ts            # Zod 验证 schemas
│   ├── errors.ts                 # 错误类定义
│   ├── utils.ts                  # 工具函数
│   ├── layout-templates.ts       # 布局模板
│   └── icon-dictionary.ts        # 图标字典
├── actions/                      # Server Actions
│   ├── link-actions.ts           # 链接 CRUD
│   ├── user-actions.ts           # 用户操作
│   ├── module-actions.ts         # 模块操作
│   └── publish-actions.ts        # 发布操作
├── stores/                       # Zustand Stores
│   ├── editor-store.ts           # 编辑器状态
│   ├── layout-store.ts           # 布局状态
│   └── auth-store.ts             # 认证状态
├── types/                        # TypeScript 类型定义
│   └── index.ts
└── __tests__/                    # 测试文件
    └── properties/               # 属性测试
```

---

## 数据模型

### User (用户)
```prisma
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  username      String    @unique
  name          String?
  bio           String?
  avatarUrl     String?
  password      String
  theme         String    @default("aurora")  // aurora | cyber | glass
  mobileLayout  Json?     // 移动端布局配置
  desktopLayout Json?     // 桌面端布局配置
  isPublished   Boolean   @default(false)
  publishedAt   DateTime?
  // 关联
  links         Link[]
  pageModules   PageModule[]
}
```

### Link (链接)
```prisma
model Link {
  id        String   @id @default(cuid())
  userId    String
  title     String
  url       String
  icon      String?  // 图标 ID
  order     Int      @default(0)
  isActive  Boolean  @default(true)
}
```

### PageModule (页面模块)
```prisma
model PageModule {
  id        String   @id @default(cuid())
  userId    String
  type      String   // 'links' | 'bio' | 'skills' | 'projects'
  title     String?
  data      Json     // 模块特定数据
  order     Int      @default(0)
  gridX     Int      @default(0)
  gridY     Int      @default(0)
  gridW     Int      @default(1)
  gridH     Int      @default(1)
}
```

---

## 核心功能模块

### 1. 用户认证
- **注册**: 邮箱 + 密码 + 用户名，密码 bcrypt 加密
- **登录**: NextAuth Credentials Provider
- **记住我**: 7天长效登录 (Zustand + localStorage)
- **路由保护**: `proxy.ts` 实现 (Next.js 16 推荐方式)

### 2. 链接管理
- **CRUD**: 创建/读取/更新/删除链接
- **拖拽排序**: @dnd-kit 实现
- **图标选择**: 预设图标字典 (国内外社交平台)
- **URL 验证**: Zod schema 验证

### 3. 主题引擎
| 主题 | 效果 |
|------|------|
| Aurora | 动画渐变背景 |
| Cyber | 霓虹边框发光 |
| Glass | 玻璃拟态 + 3D 倾斜 |

### 4. 页面模块系统
| 模块类型 | 描述 |
|----------|------|
| links | 社交链接列表 |
| bio | 个人简介 (头像/姓名/简介) |
| skills | 技能标签云 |
| projects | 项目展示卡片 |

### 5. 布局编辑器
- **react-grid-layout**: 拖拽调整模块位置和大小
- **移动端布局**: 固定宽度 375px
- **默认模板**: 新用户自动生成布局

### 6. 一键发布
- **发布**: 设置 `isPublished = true`
- **公开链接**: `/u/[username]`
- **取消发布**: 访客访问返回 404

---

## Server Actions API

### link-actions.ts
| 函数 | 功能 |
|------|------|
| `getUserLinks()` | 获取用户所有链接 |
| `createLink(data)` | 创建链接 |
| `updateLink(id, data)` | 更新链接 |
| `deleteLink(id)` | 删除链接 |
| `reorderLinks(linkIds)` | 重排序链接 |

### user-actions.ts
| 函数 | 功能 |
|------|------|
| `registerUser(data)` | 用户注册 |
| `getUserByEmail(email)` | 按邮箱查询用户 |
| `updateUserProfile(data)` | 更新个人资料 |
| `updateUserTheme(theme)` | 更新主题 |

### module-actions.ts
| 函数 | 功能 |
|------|------|
| `getModules()` | 获取用户所有模块 |
| `createModule(data)` | 创建模块 |
| `updateModule(id, data)` | 更新模块 |
| `deleteModule(id)` | 删除模块 |
| `saveLayout(layoutItems)` | 保存布局 |
| `saveDeviceLayout(mode, layout)` | 保存设备布局 |
| `getDeviceLayouts()` | 获取设备布局 |

### publish-actions.ts
| 函数 | 功能 |
|------|------|
| `publishPage()` | 发布页面 |
| `unpublishPage()` | 取消发布 |
| `getPublishStatus()` | 获取发布状态 |

---

## Zustand Stores

### editor-store.ts
管理链接编辑器状态：
- `links`: 链接数组
- `theme`: 当前主题
- `previewMode`: 预览模式
- `isDirty`: 是否有未保存更改

### layout-store.ts
管理布局编辑器状态：
- `modules`: 模块数组
- `layout`: 当前布局
- `mobileLayout`: 移动端布局
- `deviceMode`: 设备模式
- `isEditing`: 编辑模式

### auth-store.ts
管理认证状态：
- `rememberMe`: 是否记住登录
- `loginExpiry`: 登录过期时间

---

## 开发任务进度

### ✅ 已完成
- 项目初始化和基础配置
- 数据库和 Prisma 配置
- 用户认证系统 (注册/登录)
- 链接管理 CRUD
- 主题引擎 (Aurora/Cyber/Glass)
- 实时预览功能
- Dashboard 界面
- 公开页面 SSR 渲染
- 页面模块系统
- 布局编辑器
- 一键发布功能
- 属性测试 (部分)

### ⏳ 待完成
- [ ] Open Graph 元数据生成
- [ ] 性能优化 (图片/字体/JS体积)
- [ ] 长效登录 Bug 修复
- [ ] 页面管理界面重构
- [ ] 模块编辑功能完善
- [ ] 个人资料保存功能修复
- [ ] 剩余属性测试

---

## 开发命令

```bash
# 开发模式
npm run dev

# 构建
npm run build

# 运行测试
npm run test

# 数据库迁移
npx prisma migrate dev

# 重置数据库
npm run db:reset

# 数据库种子
npm run db:seed
```

---

## 环境变量

```env
DATABASE_URL="postgresql://user:password@localhost:5432/linkpro"
AUTH_SECRET="your-auth-secret"
NEXTAUTH_URL="http://localhost:3000"
```

---

## 关键文件说明

| 文件 | 说明 |
|------|------|
| `src/app/proxy.ts` | 路由保护，替代 middleware |
| `src/lib/validations.ts` | 所有 Zod 验证 schema |
| `src/lib/icon-dictionary.ts` | 图标字典配置 |
| `src/lib/layout-templates.ts` | 布局模板生成 |
| `src/components/themes/` | 三个主题组件 |
| `prisma/schema.prisma` | 数据库模型定义 |

---

## 测试策略

- **单元测试**: Vitest
- **属性测试**: fast-check (100+ 迭代)
- **测试文件位置**: `src/__tests__/`

### 已实现的属性测试
- URL 验证
- Zod 数据验证
- 用户注册
- 链接 CRUD

---

## 注意事项

1. **Next.js 16**: 使用 `proxy.ts` 替代 `middleware.ts`
2. **布局编辑器**: 当前仅支持移动端布局
3. **主题映射**: 确保 `aurora → AuroraTheme`, `glass → GlassTheme`
4. **Server Actions**: 所有数据操作通过 Server Actions 完成
5. **类型安全**: 使用 Zod 进行运行时验证

---

## 联系方式

如有问题，请参考：
- `.kiro/specs/linkpro/requirements.md` - 需求文档
- `.kiro/specs/linkpro/design.md` - 设计文档
- `.kiro/specs/linkpro/tasks.md` - 任务列表
